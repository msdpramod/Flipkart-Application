spring:
  application:
    name: Flipkart-Application

  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:mem:flipkartdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true